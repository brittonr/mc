# Tasks

- [x] [serial] Define the versioned typed event schema, source labels, required fields, redaction policy, and BLAKE3 timeline hash contract. r[mc_compatibility.typed_event_oracle.schema]
  - Evidence: `tools/mc-compat-runner/src/main.rs` defines `TypedEvent`, `TYPED_EVENT_PREFIX`, `TYPED_EVENT_SCHEMA_VERSION`, and parser validation for source/scenario/session/username/sequence/event fields. README documents `typed_event_oracle` receipt migration metadata.
- [x] [depends:schema] Add pure Rust parsing and event-graph evaluation functions with positive fixtures for at least smoke, inventory, survival, reconnect, and combat/projectile rails. r[mc_compatibility.typed_event_oracle.graph]
  - Evidence: pure functions `parse_typed_event_line`, `parse_typed_event_fields`, and `evaluate_typed_event_graph` evaluate versioned typed event timelines without I/O. Tests `typed_event_parser_accepts_versioned_event_lines`, `typed_event_graph_checks_required_forbidden_and_ordered_events`, and `typed_event_graph_accepts_representative_scenario_fixtures` cover smoke, inventory, survival, reconnect, combat, and projectile typed-event fixtures plus wrong username, forbidden event, and out-of-order causal mutations.
- [x] [depends:graph] Add negative oracle fixtures for missing events, forbidden events, wrong username/session, stale sequence, and out-of-order causal events. r[mc_compatibility.typed_event_oracle.negative_tests]
  - Evidence: `typed_event_parser_accepts_versioned_event_lines` rejects unsupported schema; `typed_event_graph_checks_required_forbidden_and_ordered_events` rejects missing required events, wrong username, wrong session, forbidden event insertion, duplicate/stale sequence order violations, and out-of-order causal edges.
- [x] [depends:schema] Emit typed client and server events from the maintained Stevenarella probes and Valence/Paper fixtures while preserving existing text logs. r[mc_compatibility.typed_event_oracle.emitters]
  - Evidence: runner-side pure function `typed_events_from_receipt_evidence` materializes typed client/server events from maintained probe/fixture milestone evidence for successful live receipts while preserving the existing text logs. Reviewable smoke sidecar: `docs/evidence/typed-event-oracle-smoke-2026-05-29.typed-events.log`.
- [x] [depends:emitters] Record event-log paths, schema version, and normalized timeline BLAKE3 in receipts; keep raw payload recording disabled by default. r[mc_compatibility.typed_event_oracle.receipts]
  - Evidence: `write_typed_event_oracle_artifact`, `normalize_typed_event_timeline`, and `typed_event_timeline_blake3` write successful live-run `.typed-events.log` sidecars and receipt metadata with schema version, event-log path, normalized BLAKE3, event count, `contributes_to_pass_fail`, and `raw_payloads_recorded=false`. Reviewable receipt: `docs/evidence/typed-event-oracle-smoke-2026-05-29.receipt.json`; manifest: `docs/evidence/typed-event-oracle-smoke-2026-05-29.b3`.
- [x] [depends:receipts] Migrate representative maintained rails from substring-only checks to typed event-graph checks, with fallback clearly marked for unmigrated rails. r[mc_compatibility.typed_event_oracle.migration]
  - Evidence: `validate_typed_event_oracle_for_migrated_scenario` makes smoke and inventory-interaction run evidence fail if the typed event graph is missing required events, sees forbidden events, or violates scenario order. `typed_event_oracle_contributes_to_pass_fail` marks migrated rails in receipts; unmigrated rails keep `migration_status="substring-fallback"`.
- [x] [depends:migration] Copy reviewable dry-run/live evidence under `docs/evidence/`, update README/current bundle guidance, run runner tests, maintained dry-run checks, evidence manifests, and `nix run --no-update-lock-file .#cairn -- validate --root .`. r[mc_compatibility.typed_event_oracle.validation]
  - Evidence: live smoke receipt, typed event sidecar, client log, Valence log, run log, and BLAKE3 manifest copied under `docs/evidence/typed-event-oracle-smoke-2026-05-29.*`. Validation log `docs/evidence/typed-event-oracle-validation-2026-05-29.run.log` records runner fmt/tests, local evidence manifest pass (`42 manifests, 212 entries, 57 receipts scanned`), individual maintained dry-run check pass (pueue task 428), `cairn validate`, and `cairn gate tasks harden-typed-event-oracle`; `docs/evidence/typed-event-oracle-validation-2026-05-29.b3` records its BLAKE3.
